global:
  scrape_interval: 1m

scrape_configs:
  - job_name: "prometheus"
    scrape_interval: 1m
    static_configs:
    - targets: ["http://localhost:9090"]

  - job_name: "postgres_metrics"
    static_configs:
    - targets: ["http://host.docker.internal:9187"]
